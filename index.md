Cosa c'è nella memoria. Lo aggiorna `/ingest` ogni volta che scrive o cambia una pagina, e serve a Claude per sapere da dove partire quando gli chiedi qualcosa.

Per ora è vuota: metti qualcosa in `memoria/grezzo/` e lancia `/ingest`.

<!-- Da qui in giù: una riga per pagina, con il collegamento e mezza riga di cosa c'è dentro. -->
